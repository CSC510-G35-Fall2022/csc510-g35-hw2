import utilities
import num as n
import sym as s
import cols
class data:
    def __init__(self, src):
        utilobj=utilities.utilities()
        cols= 'NIL'
        rows={}
        if type(src)==str:
            print("SRC IS STRING")
        else:
            for _ in range(s):
                self.add(row)

